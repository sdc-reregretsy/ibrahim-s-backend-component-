config:
  target: "http://localhost:3022"
  phases:
    - duration: 20
      arrivalRate: 11
scenarios:
  - name: "GET /"
    flow:
      - loop:
        - get:
            url: "/"
            gzip: true
        count: 100 